__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# CUDA extension build artifacts (torch.utils.cpp_extension ninja output)
kernels/build/
*.so
*.pyd
*.obj
*.lib
*.exp
*.ncu-rep

# Tokenized corpus and checkpoints — large, regenerable, never committed.
data/
checkpoints/
*.bin
*.idx
tokenizer.json

# Benchmark results ARE committed (bench/results/*.json) so the charts in
# docs/figures/ can be regenerated by anyone without a GPU. Only the raw
# profiler dumps are excluded.
bench/results/raw/

.DS_Store
.venv/
